<h2>ASP新規作成</h2>
<canvas id="canvas"><!-- ASP新規作成 --></canvas>
<div class="actions one head">
	<span class="back"><%= link_to "戻る", polymorphic_path([:admin, @client, :asps]) %></span>
</div>
<%= render 'form' %>
<div class="actions one">
	<span class="back"><%= link_to "戻る", polymorphic_path([:admin, @client, :asps]) %></span>
</div>
