<%= content_tag_for :tr, reward_point do %>
	<td><%= reward_point.name %></td>
	<td><span class="edit"><%= link_to "変更", edit_polymorphic_path([:admin, @client, @promotion, reward_point])  %></span></td>
	<td><span class="del"><%= link_to "削除", polymorphic_path([:admin, @client, @promotion, reward_point], :action => :delete) %></span></td>
<% end %>
